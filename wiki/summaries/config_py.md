# config.py

Backward-compatibility stub that re-exports `core.config`.

## What It Does

Single line: `from core.config import *`. Exists so that modules using `from config import X` continue to work after the config was moved to `core/config.py`. (Source: `config.py:1-2`)

See also: [core_config](core_config.md)
